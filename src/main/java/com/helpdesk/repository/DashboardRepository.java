/*package com.helpdesk.repository;*/

//import com.helpdesk.model.Ticket;
//import com.helpdesk.model.TicketStatus;
/*
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;


@Repository
public interface DashboardRepository extends JpaRepository <ticketId, Long>{

        long countByTicketStatus(TicketStatus ticketStatus);
        long countByTicketStatus(TicketStatus ticketStatus.OPEN);
        long countByTicketStatus(TicketStatus ticketStatus.IN_PROGRESS);
        long countByTicketStatus(TicketStatus ticketStatus.RESOLVED);
        long countByTicketStatus(TicketStatus ticketStatus.CLOSED);
}

*/